imports:
  - { resource: doctrine.yml }
  - { resource: zig.install.yml }

app.config:
  name: catapatate
  charset: UTF-8
  error_handler: null
  router: { resource: "%kernel.root_dir%/config/routing.yml" }
  validation: { enabled: true, annotations: true }
  templating: ~
    #escaping: htmlspecialchars
    #renderers:
  #session
  #translator:
  #  fallback:

twig.config: ~

zig.registry: ~

zig.api: ~

#  directory: config/registry

# disabled because "extension is not enabled" (exception)
#swift.mailer:
#  transport: smtp #gmail
#  host: localhost #smtp.dedibox.fr
  #username:
  #password:

