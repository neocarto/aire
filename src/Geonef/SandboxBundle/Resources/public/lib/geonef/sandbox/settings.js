
dojo.provide('catapatate.settings');

// deps
dojo.require('geonef.jig.workspace.fx');

(function() {
   //console.log('hehe! settings', this, arguments);
   dojo.mixin(geonef.jig.workspace.fx.color, {
                openEnd: '#ffcc00',
                close: '#ffcc00'
              });
 })();
