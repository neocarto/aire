imports:
  - { resource: config.yml }

monolog:
    handlers:
        main:
            type:  stream
            path:  %kernel.logs_dir%/%kernel.environment%.log
            level: warning

geonef_ploomap:
  geocache:
    local_wms_domain: test.v2.aire.ums-riate.fr
