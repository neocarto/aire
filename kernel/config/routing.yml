homepage:
  pattern: /
  defaults: { _bundle: ZigBundle, _controller: Workspace, _action: view, key: default }

__homepage:
  pattern:  /___
  defaults: { _bundle: WebBundle, _controller: Default, _action: index }

hurepoix:
  resource: HurepoixBundle/Resources/config/routing.yml

zig:
  resource: ZigBundle/Resources/config/routing.yml

# testz:
#   pattern: /testz
#   defaults: { _bundle: ZigBundle, _controller: Json, _action: process }
